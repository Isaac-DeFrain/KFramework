// Listening variables

    #LV( a , b <- x &
         @[\c,d!(\e /\ Int)] <- x &
         f , @for( @for( <- v ){0} <= u & @1 <= t ){ v!(*u) } <- z
       )

/*
Output:

  SetItem ( a )
  SetItem ( b )
  SetItem ( c )
  SetItem ( d )
  SetItem ( e )
  SetItem ( f )
  SetItem ( t )
  SetItem ( u )

*/
