// Free Variables - contract

    FV( new Name , z(`rho:io:stdout`) in {

      contract Name ( x , y ) = {
        @{z!(Nil)}!(*x) | for( u <- @"a" ){ *y }
      }

    } )

/*
Output:

    .Set

*/
