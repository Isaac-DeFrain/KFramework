// Free variables - kind

  FV( for( @Nil <= @true &
              y <= u &
              u <= @1 ){

        *y | *u | for( x <- y ){ *x + \y }

      } | *u )

/*
Output:

  SetItem ( u :: name )
  SetItem ( y :: proc )

*/
