// Free variables

  FV( for( @Nil <= @true ; y <= u ; u <= @1 ){ *y | *u | for( x <- y ){ *x + \y } } | *u )

/*
Output:

    SetItem ( * u : proc )
    SetItem ( \ y : proc )
    SetItem ( u : name )

*/