// Match -- MultiRec
  // incorrect flow of information in continuation

  #matchMRec( for( a,b <- @0 & c,d <- @1 ){ for( s,t <- a ){ b!([*s,*t]) } } ;
              for( a,b <- @0 & c,d <- @1 ){ for( s,t <- a ){ b!([*s,*t]) } } )

/*
Output:

  true

*/
