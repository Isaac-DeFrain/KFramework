// Untype - type[ for ( y <- @ ( { @ Nil } ! ( "nil" ) ) ) { 0 } ]

   type[ "receive" ;; type[ "bind" ;; type[ "leaf" ;; "namevar" ;; y ] ;; type[ "quote" ;; type[ "send" ;; type[ "quote" ;; type[ "leaf" ;; "nil" ;; "Nil" ] ;; type[ "leaf" ;; "truncate" ;; #truncate ] ] ;; type[ "leaf" ;; "string" ;; "nil" ] ] ;; type[ "leaf" ;; "truncate" ;; #truncate ] ] ] ;; type[ "leaf" ;; "int" ;; 0 ] ]

/*
  Output:
  untype[ for ( y <- @ ( { @ Nil } ! ( "nil" ) ) ) { 0 } ]
*/