// Receive -- wildcard listen

    new x,a in { x!(*a,41) | x!(41) | for( @{\a /\ Unforgeable},@{\c /\ Int} <- x ){ [ 1 + \c , \a ] } }

/*
Output:
<T>
  <activity>
    <task>
      <k>
        [ 42 , unforgeable( 1 ) ]
      </k>
    </task>
  </activity>
  <tuplespace>
    <sends>
      <send>
        <schan>
          @ unforgeable( 0 )
        </schan>
        <msg>
          41
        </msg>
        <arity>
          1
        </arity>
        <stype>
          0
        </stype>
      </send>
    </sends>
    <receives>
      .RecCellBag
    </receives>
  </tuplespace>
  <nextLoc>
    2
  </nextLoc>
  <original>
    new x , a in { x ! ( * a , 41 ) | x ! ( 41 ) | for ( @ \ a /\ Unforgeable , @ \ c /\ Int <- x ) { [ 1 + \ c , \ a ] } }
  </original>
  <OGfreevar>
    .Set
  </OGfreevar>
  <OGdeBruijn>
    new boundN( 0 ) , boundN( 1 ) in { boundN( 0 ) ! ( * boundN( 1 ) , 41 ) | boundN( 0 ) ! ( 41 ) | for ( @ boundP( 2 ) /\ Unforgeable , @ boundP( 3 ) /\ Int <- boundN( 0 ) ) { [ 1 + boundP( 3 ) , boundP( 2 ) ] } }
  </OGdeBruijn>
</T>
*/