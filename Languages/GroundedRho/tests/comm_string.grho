// String - race condition

    x!("Hello ") | for( y <- x ){ *y ++ "world!" }

/*
Output:
Result ==K <T>
  <activity>
    <task>
      <k>
        "Hello world!"
      </k>
    </task>
  </activity>
  <tuplespace>
    <sends>
      .SendCellBag
    </sends>
    <receives>
      .RecCellBag
    </receives>
  </tuplespace>
  <nextLoc>
    0
  </nextLoc>
  <original>
    x ! ( "Hello " ) | for ( y <- x ) { * y ++ "world!" }
  </original>
  <freeVars>
    SetItem ( x : name )
  </freeVars>
  <deBruijnified>
    x ! ( "Hello " ) | for ( boundN( 0 ) <- x ) { * boundN( 0 ) ++ "world!" }
  </deBruijnified>
</T>
*/