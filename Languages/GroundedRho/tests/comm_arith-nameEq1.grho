// Communication on name equivalent channels

    @{1 | @3!(Nil) | Nil | "a"}!(41,true) | for( @\y,y <- @{"a" | 1 | @3!(Nil)} ){ \y + 1 | not *y }

/*
Output:
<T>
  <threads>
    <thread>
      <k>
        42
      </k>
    </thread> <thread>
      <k>
        false
      </k>
    </thread>
  </threads>
  <tuplespace>
    <sends>
      .SendCellBag
    </sends>
    <receives>
      .RecCellBag
    </receives>
  </tuplespace>
  <nextLoc>
    0
  </nextLoc>
  <freeVars>
    .Set
  </freeVars>
  <deBruijn>
    ( @ 1 | ( @ 3 ) ! ( Nil ) | "a" ) ! ( 41 , true ) | for ( @ boundP( 0 ) , boundN( 1 ) <- @ "a" | 1 | ( @ 3 ) ! ( Nil ) ) { boundP( 0 ) + 1 | not * boundN( 1 ) }
  </deBruijn>
</T>
*/