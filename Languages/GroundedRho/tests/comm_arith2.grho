// send plus

    new x in { x!(41,true,@1!("the message")) | for( @\a,b,@{@{Int}!(\d)} <- x ){ \a + 1 | not *b | \d } }

/*
Output:
<T>
  <activity>
    <task>
      <k>
        42
      </k>
    </task> <task>
      <k>
        "the message"
      </k>
    </task> <task>
      <k>
        false
      </k>
    </task>
  </activity>
  <tuplespace>
    <sends>
      .SendCellBag
    </sends>
    <receives>
      .RecCellBag
    </receives>
  </tuplespace>
  <nextLoc>
    1
  </nextLoc>
  <original>
    new x in { x ! ( 41 , ( true , @ 1 ! ( "the message" ) ) ) | for ( @ \ a , b , @ ( @ Int ! ( \ d ) ) <- x ) { \ a + 1 | not * b | \ d } }
  </original>
  <OGfreevar>
    .Set
  </OGfreevar>
  <OGdeBruijn>
    new boundN( 0 ) in { boundN( 0 ) ! ( 41 , true , @ 1 ! ( "the message" ) ) | for ( @ boundP( 1 ) , boundN( 2 ) , @ ( @ Int ! ( boundP( 3 ) ) ) <- boundN( 0 ) ) { boundP( 1 ) + 1 | not * boundN( 2 ) | boundP( 3 ) } }
  </OGdeBruijn>
</T>
*/