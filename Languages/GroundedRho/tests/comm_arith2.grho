// send plus

  new x in { x!(41,true) | for( @\y,y <- x ){ \y + 1 | not *y } }

/*
Output:
<T>
  <threads>
    <thread>
      <k>
        42
      </k>
    </thread> <thread>
      <k>
        false
      </k>
    </thread>
  </threads>
  <tuplespace>
    <sends>
      .SendCellBag
    </sends>
    <receives>
      .RecCellBag
    </receives>
  </tuplespace>
  <nextLoc>
    1
  </nextLoc>
  <freeVars>
    .Set
  </freeVars>
  <deBruijn>
    new boundN( 0 ) in { boundN( 0 ) ! ( 41 , true ) | for ( @ boundP( 1 ) , boundN( 2 ) <- boundN( 0 ) ) { boundP( 1 ) + 1 | not * boundN( 2 ) } }
  </deBruijn>
</T>
*/