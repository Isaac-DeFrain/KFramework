// AExp - guard

    new x in { x!(41) | for( y <- x if *y > 40 ){ *y + 1 } }

/*
Output:
<T>
  <threads>
    <thread>
      <k>
        42
      </k>
    </thread>
  </threads>
  <tuplespace>
    <sends>
      .SendCellBag
    </sends>
    <receives>
      .RecCellBag
    </receives>
  </tuplespace>
  <nextLoc>
    1
  </nextLoc>
  <freeVars>
    .Set
  </freeVars>
  <deBruijn>
    new boundN( 0 ) in { boundN( 0 ) ! ( 41 ) | #deBruijn( 1 ; for ( y <- boundN( 0 ) if * y > 40 ) { * y + 1 } ; x |-> boundN( 0 ) ) }
  </deBruijn>
</T>
*/