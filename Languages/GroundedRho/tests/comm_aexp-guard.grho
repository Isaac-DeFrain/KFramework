// Input guard - comm

    for( @\y <- @Nil if \y < 38 and \y > 36 ){ \y + 5 } | @Nil!(36) | @Nil!(37) | @Nil!(38)

/*
Output:
<T>
  <activity>
    <task>
      <k>
        42
      </k>
    </task>
  </activity>
  <tuplespace>
    <sends>
      <send>
        <schan>
          @ Nil
        </schan>
        <msg>
          36
        </msg>
        <arity>
          1
        </arity>
        <stype>
          0
        </stype>
      </send> <send>
        <schan>
          @ Nil
        </schan>
        <msg>
          38
        </msg>
        <arity>
          1
        </arity>
        <stype>
          0
        </stype>
      </send>
    </sends>
    <receives>
      .RecCellBag
    </receives>
  </tuplespace>
  <nextLoc>
    0
  </nextLoc>
  <original>
    for ( @ \ y <- @ Nil if \ y < 38 and \ y > 36 ) { \ y + 5 } | ( @ Nil ) ! ( 36 ) | ( @ Nil ) ! ( 37 ) | ( @ Nil ) ! ( 38 )
  </original>
  <OGfreevar>
    .Set
  </OGfreevar>
  <OGdeBruijn>
    for ( @ boundP( 0 ) <- @ Nil if boundP( 0 ) < 38 and boundP( 0 ) > 36 ) { boundP( 0 ) + 5 } | ( @ Nil ) ! ( 36 ) | ( @ Nil ) ! ( 37 ) | ( @ Nil ) ! ( 38 )
  </OGdeBruijn>
</T>
*/