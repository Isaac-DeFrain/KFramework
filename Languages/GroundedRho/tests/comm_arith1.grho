// send plus

    new x in { x!(41) | for( @\y <- x ){ \y + 1 } }

/*
Output:
<T>
  <threads>
    <thread>
      <k>
        42
      </k>
    </thread>
  </threads>
  <tuplespace>
    <sends>
      .SendCellBag
    </sends>
    <receives>
      .RecCellBag
    </receives>
  </tuplespace>
  <nextLoc>
    1
  </nextLoc>
  <freeVars>
    .Set
  </freeVars>
  <deBruijn>
    new boundN( 0 ) in { boundN( 0 ) ! ( 41 ) | for ( @ boundP( 1 ) <- boundN( 0 ) ) { boundP( 1 ) + 1 } }
  </deBruijn>
</T>
*/