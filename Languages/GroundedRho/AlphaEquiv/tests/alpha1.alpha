// Alpha equivalence
// these processes are structurally equivalent, but *not* alpha equivalent

    { 0 | for( b <- @1 ){ *b } }

      #alpha

    { for( b <- @1 ){ *b } | 0 }

/*
Output:

  false

*/
