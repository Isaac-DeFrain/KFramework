// De Bruijn levels

    #deBruijn( new x,a,b in { x!(for( y <- @1 ){ *y | *x | for( b <- a ){ for( c <- b ){ *b | *c } } } ) | a!(*b) } )
  
/* Output: 
   
    new boundN( 0 ) , boundN( 1 ) , boundN( 2 ) in {

      boundN( 0 ) ! ( for ( boundN( 3 ) <- @ 1 ) { 
        * boundN( 3 ) | * boundN( 0 ) | for ( boundN( 4 ) <- boundN( 1 ) ) {
          for ( boundN( 5 ) <- boundN( 4 ) ) {
            * boundN( 4 ) | * boundN( 5 )
          } 
        } 
      } ) | 
      boundN( 1 ) ! ( * boundN( 2 ) ) 
    }

*/