// Alpha equivalence

    new a,b,c,d in { *d | *c | *b | *a | for( e,f <- a ){ *e + *f } }

      #alpha

    new w,x,y,z in { *z | *y | *x | *w | for( u,v <- w ){ *u + *v } }
  
/*
Output:

    true

*/