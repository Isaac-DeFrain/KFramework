// Multi receive indexing

    #deBruijn( for(  @\c,@{d!(3)} <= @0 & @\a <= @1 & <= @{for( @\a,@\b,@\c <- @0 ){ [\a,\b,\c] }} ){ \a | \c } )

/*
Output:

  for ( @ x( 0 ) , @ ( { @ x( 1 ) } ! ( 3 ) ) <= @ 0 & @ x( 2 ) <= @ 1 & <= @ for ( @ x( 0 ) , @ x( 1 ) , @ x( 2 ) <- @ 0 ) {
    [ x( 0 ) , x( 1 ) , x( 2 ) ] } ) { x( 2 ) | x( 0 )
  }

RNode:

  for( @{x0}, @{@{x1}!(3)} <= @{0} ;
       @{x2} <= @{1} ;
       @{@{x3}!(0)} <= @{for( @{x0}, @{x1}, @{x2} <- @{0} ) { [x0, x1, x2] }} ) {
    x3 | x2 | x0
  }

*/
