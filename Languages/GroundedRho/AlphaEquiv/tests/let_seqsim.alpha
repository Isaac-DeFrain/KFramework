// LetExp

    #deBruijn( let \a <- 0 & \b <- @0!("a") > \c <- [\a,\b] > \d <- 1 ; \e <- 2  in { [\a,\b,\c,\d,\e] } )

/*
Output:

  let x( 0 ) <- 0 & x( 1 ) <- { @ 0 } ! ( "a" ) > x( 1 ) <- [ x( 0 ) , x( 1 ) ] > x( 2 ) <- 1 ; ( x( 3 ) <- 2 ) in { [ x( 0 ) , x( 1 ) , x( 2 ) , x( 3 ) , x( 4 ) ] }

*/
