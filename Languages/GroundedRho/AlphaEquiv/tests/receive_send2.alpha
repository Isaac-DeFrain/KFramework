// De Bruijn levels - receive a send

    #deBruijn( for( @{x!!(*z)},y <- @Nil ){ z!(*x) | *y } )

/*
Output:

  for ( @ ( { @ x( 0 ) } !! ( x( 1 ) ) ) , @ x( 2 ) <- @ Nil ) { { @ x( 1 ) } ! ( x( 0 ) ) | x( 2 ) }

*/
