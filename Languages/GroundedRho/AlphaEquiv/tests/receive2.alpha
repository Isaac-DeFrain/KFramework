// Receive scoping

    new x in { for( @\x <- x ){ \x } }
  
/*
Output: 

    new boundN( 0 ) in { for ( @ boundP( 1 ) <- boundN( 0 ) ) { boundP( 1 ) } }

RNode:

    new x0 in { for( @{x1} <- x0 ) { x1 } }

*/