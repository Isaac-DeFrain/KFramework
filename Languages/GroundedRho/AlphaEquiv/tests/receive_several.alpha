// De Bruijn levels - receive several names

    #deBruijn( for( @{\x /\ Int},a,b,@\c <- @Nil ){ \x | a!(\c | *b ) } )

/*
Output:

  for ( @ x( 0 ) /\ Int , @ x( 1 ) , @ x( 2 ) , @ x( 3 ) <- @ Nil ) { x( 0 ) | { @ x( 1 ) } ! ( x( 3 ) | x( 2 ) ) }

*/
