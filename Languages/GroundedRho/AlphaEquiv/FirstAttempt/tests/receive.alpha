// Receive scoping

    new x,y,z in { for( @\a <- x ){ new x in { *x } | new y in { *y } | \a } }
  
/*
Output: 

    new boundN( 0 ) , boundN( 1 ) , boundN( 2 ) in { for ( @ boundP( 3 ) <- boundN( 0 ) ) { new boundN( 4 ) in { * boundN( 4 ) } | new boundN( 4 ) in { * boundN( 4 ) } | boundP( 3 ) } }

RNode:

    new x0, x1, x2 in { for( @{x3} <- x0 ){ new x4 in { *x4 } | new x4 in { *x4 } | x3 } }

*/