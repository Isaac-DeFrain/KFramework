// Matching Binds Set -- Rec inputs

  #matchBindSet( for( @[\a,\b] <<- @0 & c <<- @0 ){ [\a,\b] } ;; for( a <<- @0 & b <<- @0 ){ *b } )

/*
Output:

  SetItem ( 0 ; SetItem ( 0 ) SetItem ( 1 ) )
  SetItem ( 1 ; SetItem ( 0 ) SetItem ( 1 ) )

*/
